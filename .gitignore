# Build outputs
.next
out
build

# Node
node_modules
npm-debug.log*

# TypeScript cache
*.tsbuildinfo

# Rogue build artifact (2MB Next.js bundle that leaked to root)
index.js

# Environment & secrets
.env
.env.local
.env.*.local

# Dev server logs
.next-dev*.out.log
.next-dev*.log
*.next-dev.log
dev-server.log
dev-server.err
next-dev.log

# OS / editor
.DS_Store

# s8.8 (2026-07-31): backups pre-migration de docker (~220MB total)
backups/

# s8.8 (2026-07-31) + reconciliación 2026-07-29: mockup V0 del Figma.
# Originalmente en `docs/fumigation-management-dashboard/`, movido a
# `docs/v0-2026-07-28/` para que no confunda a `arch:check` ni a
# nuevos devs. Sigue gitignored porque es referencia histórica (no se
# ejecuta, no se commitea). El código de producción vive en
# `app/`, `components/`, `lib/`, `api/`.
docs/fumigation-management-dashboard/
docs/v0-2026-07-28/

# s8.8 (2026-07-31): preview HTML estatico generado por scripts/generate-polygon-preview.js
# Output de un script, no se commitea.
public/polygon-preview.html

# s8.8 (2026-07-31): scripts de diagnostico one-shot (no operacionales).
# Se conservan en el repo local para referencia pero no se commitean.
scripts/align-docker-*.js
scripts/create-supabase-extra-tables.js
scripts/fix-sequences.js
scripts/generate-polygon-preview.js
scripts/simulate-v0-export.js
scripts/sync-param-waypoint-to-supabase.js
scripts/sync-polygons-to-supabase.js
scripts/test-queries-supabase.js
scripts/migrate-docker-to-supabase.js
# (scripts/backfill-lands-metadata.js, scripts/backup-djiag-health.js:
#  son operacionales — SE COMMITEAN)
Thumbs.db
.vscode/
.idea/

# Test coverage
coverage/

# ============================================================
# DJI AG data â€” user-specific (NEVER commit client DJI data)
# ============================================================
djiag_exports/

# ============================================================
# DJI credentials (filled from DJI_CREDENTIALS.md.template)
# Never commit the real credentials file.
# ============================================================
docs/DJI_CREDENTIALS.md

# ============================================================
# Scratch / debug output at repo root
# (manual page captures, API response samples, console logs)
# ============================================================
/_dashboard.html
/_parcel.html
/_fumigations.json
/_test_out.txt
/aggr_by_day.json
/log.txt

# ============================================================
# Playwright artifacts
# ============================================================
djiag_session.json
test-results/
playwright-report/
.playwright/

# ============================================================
# Sprint one-off diagnostic / debug scripts (kept on disk,
# not part of MVP â€” recreated on demand if needed)
# ============================================================
/scripts/diag-*.js
/scripts/debug-*.js
/scripts/capture-aggr-from-records.js

# ============================================================
# Local-only artifacts (dev server logs / pidfile / plan state)
# ============================================================
/tmp-dev.log
/tmp-dev-err.log
/tmp-dev.pid
/.mavis/

# ============================================================
# Database backups (Sprint C — H3a)
# Generados por `node scripts/db-backup.js` (weekly, 7-day rotation).
# Los dumps son binarios grandes (~5-50 MB gzip) con data del
# cliente — NUNCA se commitean. Para offsite, copiar manualmente
# a S3 / Supabase storage / disco externo.
# ============================================================
/backups/
.vercel

# ============================================================
# s8.9 / S6 (2026-07-31): one-off tmp-* diagnostic artifacts
# at repo root. Scripts, logs, and err files left behind by
# sprints (e.g. feature/reports-level-2). Never operational —
# recreated on demand if needed. Stash dir holds history.
# ============================================================
/tmp-*.js
/tmp-*.log
/tmp-*.err
/tmp-trash/

# ============================================================
# S6 / diagrams (2026-08-12): local clone of the
# cathrynlavery/diagram-design skill (used to author the
# diagrams in docs/diagrams/). NOT committed — the skill is
# re-cloned on demand by reading docs/diagrams/style-guide.md
# (the project-owned brand skin override) and following
# docs/diagrams/README.md "Cómo agregar un diagrama nuevo".
# Also ignores the render previews (NN-preview.png) and the
# render script's working dir artifacts.
# ============================================================
/.skills/

